security:
  oauth2:
    client:
      clientId: bucket-service
      clientSecret: secret
      accessTokenUri: http://auth-service:5000/uaa/oauth/token
      grant-type: client_credentials
      scope: server

server:
  context-path: /bucket
  port: 8000